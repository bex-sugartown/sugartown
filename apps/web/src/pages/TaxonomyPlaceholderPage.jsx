/**
 * TaxonomyPlaceholderPage — re-export shell.
 *
 * Replaced by TaxonomyDetailPage in Epic 3 (taxonomy detail pages).
 * This shell keeps App.jsx imports stable — no route changes required.
 */
export { default } from './TaxonomyDetailPage'
